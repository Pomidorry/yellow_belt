#ifndef SUM_REVERSE_SORT_H_INCLUDED
#define SUM_REVERSE_SORT_H_INCLUDED

#pragma once
#include <string>
#include <vector>
using namespace std;
int Sum(int x, int y);
string Reverse(string s);
void Sort(vector<int>& nums);


#endif // SUM_REVERSE_SORT_H_INCLUDED
